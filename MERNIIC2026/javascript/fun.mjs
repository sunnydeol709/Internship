export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

export const mul = (a, b) => {
  return a * b;
};

export const div = (a, b) => {
  return a / b;
};

//let funcs =  {
//     add: add(),
//     sub: sub(),
//     div: div(),
//     mul: mul()
//  }

// const { add, sub, div, mul } = funcs;
